# Payload के peer-deps npm के strict resolver से टकराते हैं।
# Payload's peer dependency ranges are stricter than npm's resolver likes.
# Installs run from the repo root, so this must live here — an .npmrc inside
# apps/cms would never be read by a workspace install.
legacy-peer-deps=true
